# ───────────────── Core Web Stack ─────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0          # includes httptools, uvloop

# ───────────────── LangChain & Together ───────────
langchain-together==0.1.1
langchain-core>=0.1.52,<0.2.0
langchain-text-splitters==0.0.1

# ───────────────── General Utilities ──────────────
pydantic==1.10.17
requests==2.31.0
beautifulsoup4==4.12.2
python-multipart==0.0.6
PyPDF2==3.0.1
python-docx==0.8.11
numpy==1.24.4                    # required by torch & sklearn

# ───────────────── OPTIONAL (remove if not needed) ─
scikit-learn==1.3.2              # ~330 MB; delete if unused

# ───────────────── Local Embeddings ────────────────
# 1) light, CPU-only PyTorch build
torch==2.2.0+cpu                 \
torchaudio==2.2.0+cpu             |  install with extra index:
torchvision==0.17.0+cpu          /   https://download.pytorch.org/whl/cpu
# 2) sentence-transformers wrapper
sentence-transformers==2.6.1
